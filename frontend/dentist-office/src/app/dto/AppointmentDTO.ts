export interface AppointmentDTO {
    startDate : string;
    startTime : string;
    patient: string;
    duration: number;
}

export interface AppointmentIdDTO {
    id: number;
    startDate : string;
    startTime : string;
    patient: string;
    duration: number;
}